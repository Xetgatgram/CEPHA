# Maven build output
target/

# IDE
.idea/
*.iml
.vscode/

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Ansible Vault secrets (project-specific, never committed - see
# deploy/ansible/inventories/cloud_vms/group_vars/vault.yml.example)
deploy/ansible/inventories/cloud_vms/group_vars/vault.yml

# Docker Compose local secrets (project-specific, never committed - see
# deploy/compose/.env.example)
deploy/compose/.env
